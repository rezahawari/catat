import 'package:flutter/material.dart';

class AppColors {
  // Light mode
  static const lightBackground = Color(0xFFFAFAF8); // off-white hangat
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightCard = Color(0xFFFFFFFF);
  static const lightBorder = Color(0xFFE8E6E1);
  static const textPrimary = Color(0xFF1C1C1A);
  static const textSecondary = Color(0xFF6B6B65);
  static const textMuted = Color(0xFF9E9E98);

  // Dark mode
  static const darkBackground = Color(0xFF121212);
  static const darkSurface = Color(0xFF1A1A1A);
  static const darkCard = Color(0xFF222222);
  static const darkBorder = Color(0xFF2E2E2E);
  static const textPrimaryDark = Color(0xFFF2F2EF);
  static const textSecondaryDark = Color(0xFFA0A09A);
  static const textMutedDark = Color(0xFF6E6E68);

  // Aksen — deep teal
  static const accent = Color(0xFF0F5C55);
  static const accentSoft = Color(0xFFD9EBE9);
  static const accentDark = Color(0xFF1F8579);
  static const accentGlow = Color(0x331F8579);

  // Business Accent (Gold/Amber)
  static const businessAccent = Color(0xFFB8860B);
  static const businessSoft = Color(0xFFFAF0D7);
  static const businessDark = Color(0xFFD4AF37);

  // Status — Calm, non-alarmist palette
  static const positive = Color(0xFF2E7D62); // Income / Surplus
  static const positiveSoft = Color(0xFFE2F2EC);
  static const negative = Color(0xFFB3543F); // Expense / Defisit (Terracotta)
  static const negativeSoft = Color(0xFFF9EAE7);
  static const warning = Color(0xFFC08A3E);
  static const warningSoft = Color(0xFFFDF4E7);
  static const info = Color(0xFF386FA4);
  static const infoSoft = Color(0xFFE4F0F9);
}
